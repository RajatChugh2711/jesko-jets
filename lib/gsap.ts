// Central GSAP export — import from here everywhere.
// Plugin registration happens inside each component's useEffect
// to guarantee we're running on the client only.
export { gsap } from "gsap";
export { ScrollTrigger } from "gsap/ScrollTrigger";
